# Task 004 Evidence: Sync and Merge Data-Integrity Repairs

Completed: 2026-08-31T03:32:17Z

Approved plan SHA-256: `c6b9b58c39c4c9e9a14d79451097e7402f3faab7b9ea1fb5185004f3b423cd7d`

## Repair and primary review

- Fixed `HIDLINS-DATA-001`, the only Task-004 ledger finding.
- Replaced raw pool-id history deduplication with semantic version matching: all non-attachment entry content plus resolved attachment names, protection state, and bytes.
- Snapshot attachment storage is bounded to one `Arc<Zeroizing<Vec<u8>>>` per source binary-pool item, so repeated historical references neither retain unsanitized scratch buffers nor multiply attachment memory by history depth.
- Added a fail-closed check for replicas that claim identical version metadata while resolving the same attachment reference to different bytes. The resulting `MergeError::Unresolvable` contains only the entry UUID.
- Repaired every matching loser and pre-existing history version before current-value propagation, then rebuilt attachment reverse references.
- Extended the audited, exact-version Keepass 0.13.25 vendor patch with mutable historical access, fail-closed pool lookup, attachment-name and parent-id inspection, history-index-aware removal, and deterministic current/history attachment-reference rebuilding after parse, merge, tracked edits, and Hidlins repair. `tools/dev/vendor.py` applies the patch reproducibly, rejects unknown versions/contexts, and refreshes Cargo checksums.
- No dependency, lockfile, license allowance, advisory exception, unsafe layout access, plaintext intermediate serialization, or cryptographic implementation was added.

Primary review traced KDBX XML serialization, inner-header binary ids, parsed-database back-reference reconstruction, tracked history insertion, upstream merge/backfill ordering, repeated-sync deduplication, current add/replace/remove propagation, and orphan/reference cleanup. It also reduced the initial snapshot design from one byte copy per version reference to one zeroizing copy per pool item.

## Red/green evidence

The fail-before regression
`losing_history_attachment_bytes_survive_encrypted_save_and_reopen` created colliding pool id 0 on the two replicas, merged with local as winner, encrypted the result, reopened it, and failed with:

```text
left: Some([76, 79, 67, 65, 76, 45, 66, 89, 84, 69, 83])
right: Some([82, 69, 77, 79, 84, 69, 45, 66, 89, 84, 69, 83])
```

Those bytes are `LOCAL-BYTES` versus the required `REMOTE-BYTES`, proving the historical remote name silently aliased the local current binary. After repair the same exact encrypted save/reopen test passes.

Additional green regressions cover:

- loser-history attachment add, replacement, and removal in one encrypted save/reopen matrix;
- removal from a parsed current entry without deleting the same binary still referenced by tracked history;
- equal version metadata with unequal attachment bytes failing closed;
- current attachment add/replace/remove, remote-only additions, local-winner behavior, and repeated-reconcile idempotence;
- KeePassXC XML export retaining the loser history attachment reference and KeePassXC byte-exact export of the winner attachment.

## Task validation results

| Command | Result |
| --- | --- |
| `cargo test -p hidlins-sync --offline --locked` | PASS with loopback permission for the five mock-IMDS tests: 172 unit tests, 4 fault-injection tests, 7 property/fuzz tests, 26 merge-semantic tests, US-040–046 integrations, SigV4 corpus, and docs |
| `make test-sigv4` | PASS; AWS-published corpus matched |
| `make test-merge-properties` | PASS; 4,096 cases per proptest property |
| `make interop-sync` | PASS; KeePassXC read winner attachment bytes and retained the loser history attachment reference |
| `cargo test -p hidlins-api --test us_095_bootstrap_change_password change_master_password_reencrypts_sync_credentials --offline --locked -- --exact` | PASS; existing password-change integration re-encrypted RST-CRED-1 credentials |
| `make vendor-patches` | PASS; exact-version patches and checksums reproducible |
| `git diff --check` | PASS |

The first two `make check` attempts stopped only on the new transition test exceeding the repository's 100-line function lint (105, then 101 lines). Repeated fixtures and assertions were extracted without an allow attribute; the final full gate passed.

## Standard gate results

| Command | Result |
| --- | --- |
| `make check` | PASS; format, warning-denying clippy, workspace build/tests, macOS IOKit containment, clipboard/API/logind feature gates, and warning-denying no-default security build |
| `make deny` | PASS: advisories, bans, licenses, and sources all OK |
| `make audit` | PASS; 467 dependencies scanned against 1,226 RustSec advisories |
| `make doc` | PASS with rustdoc warnings denied |

The pre-existing cargo-deny informational warnings remain exactly as documented in Task 001 evidence: `allo-isolate`'s verified Apache-2.0 license file, upstream-driven duplicates, and the unmatched Unicode-DFS allowance. No warning or exception was added by Task 004.
