# Task 001 Evidence: Guardrails, Supply-Chain Remediation, and Completion Audit

Completed: 2026-08-31T02:44:45Z

Approved plan SHA-256: `c6b9b58c39c4c9e9a14d79451097e7402f3faab7b9ea1fb5185004f3b423cd7d`

## Implementation and review

- Root `AGENTS.md` now makes test-before-fix red/green evidence, characterization before structural change, active dependency minimization, behavior pins before dependency removal, and pre-use absolute license verification operational requirements.
- `.ai/workflow/findings.md` records the completion verdict for every included feature/bucket and recorded bug, crate-by-crate correctness review results, whole-code `$arch-reviewer` results for all six non-Flutter crates and their boundaries, unique finding IDs, concrete costs, repair directions, and task ownership.
- `.ai/workflow/dependency-review.md` records every included direct dependency, source/license, purpose, footprint, maintenance/adoption signal, alternatives, and keep/remove/replace decision. The final review found no forbidden license in the accepted graph.
- Removed the unused direct TUI `anyhow` edge and its stale bootstrap documentation.
- Removed `hex` from Hidlins manifests, the complete resolved graph, `Cargo.lock`, and `vendor/`. First-party lowercase encoding and independent test-oracle implementations cover Hidlins' fixed need. Four version-pinned, exact-context, fail-closed vendor patches replace similarly trivial downstream uses without modifying crypto, KDBX, DBus, or terminal state machines.
- Added reproducible `make vendor` / offline `make vendor-patches` automation with checksum regeneration and post-patch locked metadata validation. Narrow `.gitattributes` rules preserve published vendored CRLF/Markdown bytes without hiding source diffs behind line-ending rewrites.
- Upgraded `event-listener 5.4.1 -> 5.4.2`, `ratatui 0.30.0 -> 0.30.2` / `lru 0.16.4 -> 0.18.3`, and `keepass 0.12.9 -> 0.13.25` / yanked `chacha20 0.10.0 -> 0.10.2` using released compatible parents. No new advisory suppression was added.
- Lifted the untrusted KDBX runtime path to `quick-xml 0.41.0`; the only retained advisory path is `wayland-scanner -> quick-xml 0.39.4`, which parses trusted crate-shipped XML during build and has no published fixed parent.
- Verified `vendor/allo-isolate/LICENSE` is Apache-2.0 and its manifest points to it with `license-file`. The upstream missing-SPDX-field warning is documented and not converted into an invented exception.
- Primary-thread final review found no unresolved Task-001-scope correctness, architecture, security, licensing, or reproducibility defect. Broader audited findings remain assigned to Tasks 002-008.

## Test-forward evidence

- Lowercase hexadecimal behavior was characterized against the old `hex` dependency for empty input, every nibble, AWS SigV4 published outputs, and persisted sync hashes before replacement; the same tests pass with the local helpers.
- Legacy raw and XML-v2 keyfile hexadecimal behavior was characterized before the downstream Keepass patch. The final mixed-case/whitespace equivalence regression passes through a real vault create/unlock cycle.
- The Keepass upgrade exposed a real KeePassXC KDBX 4.0 write-back regression. `make interop` first failed with `UnsupportedVersion`; focused KDBX3 and KDBX4.0 save tests then reproduced the same failure. After normalizing legacy writable versions to current KDBX4 while preserving the parsed cipher/KDF configuration, both unit regressions and all three KeePassXC interop suites pass.
- Existing TUI snapshots and the 402-test TUI suite characterize and protect the Ratatui parent upgrade. The supported logind compile gate protects the `event-listener` change.
- Stand-alone unit tests for the patched zbus and termwiz codecs cover all nibbles, mixed case, odd length, and invalid-character reporting and pass.

## Task validation results

| Command | Result |
| --- | --- |
| `test -s .ai/workflow/findings.md` | PASS |
| `test -s .ai/workflow/dependency-review.md` | PASS |
| `rg -n "test-forward|red.*green|dependency|license" AGENTS.md` | PASS |
| `git diff --check` | PASS |
| `cargo tree --workspace --offline --locked` | PASS; resolved graph shows `event-listener 5.4.2`, `lru 0.18.3`, `chacha20 0.10.2`, and no `hex` package |
| `make vendor-patches` | PASS; exact audited patches and checksums consistent offline |
| `make test-sigv4` | PASS; AWS-published corpus |
| `make test-merge-properties` | PASS; 7 properties at 4,096 cases |
| `make snapshots-check` | PASS |
| `make interop` | PASS; US-090/091/092 |
| `make interop-entry` | PASS; US-010/012/013/014/016/018. Optional `oathtool` cross-check skipped because the executable is absent; KeePassXC assertions passed. |
| `make interop-sync` | PASS; collision winner plus loser history verified by KeePassXC |
| `make deny` | PASS: advisories, bans, licenses, and sources all OK |
| `make audit` | PASS; 467 locked dependencies scanned |

## Standard gate results

| Command | Result |
| --- | --- |
| `make check` | PASS; full format, clippy `-D warnings`, build, workspace tests, cross-target checks, and supported feature containment |
| `make deny` | PASS |
| `make audit` | PASS |
| `make doc` | PASS with `RUSTDOCFLAGS=-D warnings` |

`make check` still emits the separately audited `ZeroizingString::new` dead-code warning only in the explicit no-default security containment command; the Makefile currently permits that warning. It is finding `HIDLINS-BUILD-001`, owned by Task 003, and is not hidden or claimed fixed here.

`make deny` emits informational duplicate-version reports, the documented `allo-isolate` missing manifest SPDX-field warning, and an unused allow-list entry warning. Its four enforced checks all pass, and no license/advisory policy was weakened.

Final plan-integrity check reproduced the approved hash exactly. `git diff --check` passed after the final vendor reproduction run.
