# Evidence — Task 008

## Implementation

- Implemented by: main
- Attempt count: 1
- Added three scripted in-process TUI journeys and four accessibility semantic
  contracts over the real `App` event and Ratatui render seams.
- Added a shared test-only KDBX fixture/render helper and the discoverable
  `make test-tui-contracts` target without adding or upgrading a dependency.
- Mapped every superseded tui-skeleton and tui-enhancements manual verification
  ID to automated evidence or one narrowly retained Task 009 environment case.

## Files changed

- `crates/hidlins-tui/src/lib.rs`
- `crates/hidlins-tui/src/test_support.rs`
- `crates/hidlins-tui/src/journey_tests.rs`
- `crates/hidlins-tui/src/accessibility_contract_tests.rs`
- `Makefile`
- `.ai/workflow/tui-verification-coverage.md`

## Acceptance criteria

- [x] Scripted in-process journeys render and assert representative former
  display-suite flows.
  - Evidence: three `journey_tests` cover unlock/browse/search/lock,
    discoverability/palette/search at 60×16, visual selection, read-only,
    injected mouse, disabled mouse, keyboard navigation, and Settings.
- [x] Accessibility contracts assert important labels and states from actual
  Ratatui buffers.
  - Evidence: four `accessibility_contract_tests` cover unlock, detail, search,
    workspace, Settings, locked, palette, read-only, visual, selected, expired,
    and constrained-size surfaces.
- [x] Secret canaries are absent while password fields have a masked carrier.
  - Evidence: the master and entry canaries are rejected at every reviewable
    checkpoint; unlock and detail frames require `••••`.
- [x] Required semantics do not depend solely on color.
  - Evidence: color and accessible/monochrome renders of the same state have
    identical semantic transcripts, with textual carriers for active tab,
    visual selection, read-only, expired, search scope, and lock state.
- [x] Reviewable output contains no raw terminal controls.
  - Evidence: the raw buffer is checked before trailing-space/row
    normalization and rejects ESC plus all C0/C1 controls except row newlines.
- [x] Every removed manual test ID has a disposition.
  - Evidence: `.ai/workflow/tui-verification-coverage.md` maps both former
    display and screen-reader suites to named automation or Task 009 cases.
- [x] The focused Make target is discoverable and covered by ordinary tests.
  - Evidence: `make test-tui-contracts` passed all seven tests; `make check`
    passed the full workspace including 412 TUI tests with four ignored.
- [x] No Task 008 dependency-surface change was introduced.
  - Evidence: the Revision 5 aggregate manifest/lock/vendor fingerprint matched
    `e8305fdc948cc1f58b189a88566c1e5f237c2e3827c4672853ea0f3e1b4e2804`.
- [x] Interrupted live-service evidence is preserved honestly.
  - Evidence: the results below retain passes and environment limitations
    without relabeling the Linux logind halt.

## Preserved live-service and interop evidence

These results were collected during the interrupted first Task 008 execution.
Revision 4 superseded the flawed manual suite; it did not discard these valid
environment results.

| Surface | Result | Evidence |
| --- | --- | --- |
| MinIO startup | PASS | Pinned `minio/minio:RELEASE.2025-09-07T16-13-09Z` became healthy on the isolated localhost endpoint |
| `make test-s3-integration` | PASS | 11 `hidlins-sync` MinIO cases plus the CLI first-seed/already-in-sync case passed |
| MinIO cleanup | PASS | `hidlins-minio` container was removed immediately after the suite |
| `make test-clipboard` | PASS | Three real clipboard ownership/TTL/no-stomp cases plus the CLI handoff/block-until-clear case passed |
| `make test-os-events` | HOST ENVIRONMENT HALT | Both Linux logind cases compiled and ran, but this process had no usable system bus at the resolved socket and was not in a logind session; no product assertion was reached |
| `make interop` | PASS | US-090 Rust→KeePassXC, US-091 KeePassXC→Rust, and US-092 round trip passed |
| `make interop-entry` | PASS WITH OPTIONAL TOOL GAP | US-010/012/013/014/016/018 passed; optional `oathtool` cross-check was skipped because the executable was absent |
| `make interop-sync` | PASS | KeePassXC confirmed the winner attachment in current state and loser attachment in KDBX history |

The Linux logind halt was diagnosed precisely: the resolved system-bus socket
`/home/linuxbrew/.linuxbrew/var/run/dbus/system_bus_socket` did not exist, and
the helper PID belonged to no active logind session. This is environment
evidence, not a product pass or failure.

No old manual display or screen-reader procedure was executed. Revision 4
explicitly replaced deterministic manual checks with the automation above;
Task 009 owns removal of the obsolete documents and the compact non-gating
real-environment matrix.

## Main-thread review

The harness remains under `#[cfg(test)]`, uses real KDBX/registry fixtures,
drives production event and render seams, and does not widen production APIs.
The buffer helper preserves row order and examines raw contents before semantic
normalization. Assertions focus on stable meaning instead of full-screen
spacing. Review found no high-confidence defect or accidental scope expansion.

## Task-specific validation

| Command | Result |
| --- | --- |
| `make test-tui-contracts` | PASS: three journeys and four accessibility contracts |
| `make snapshots-check` | PASS: all goldens present and unchanged |
| `make check` | PASS: format, lint, build, workspace tests, doctests, cross-target and feature gates |
| Revision 5 dependency fingerprint command | PASS: aggregate digest matched `e8305f…2804` |

## Standard quality gate

| Command | Result |
| --- | --- |
| `make check` | PASS |
| `make deny` | PASS: advisories, bans, licenses, and sources OK; informational duplicate/unmatched-allow warnings remain |
| `make audit` | PASS: 467 locked dependencies scanned with no vulnerability finding |
| `make doc` | PASS with rustdoc warnings denied |

## Remaining concerns

The TestBackend validates Hidlins' emitted terminal-cell semantics, not terminal
emulator AT-SPI exposure or speech-engine behavior. Task 009 retains those
narrow real-environment checks as non-gating platform confidence work.
