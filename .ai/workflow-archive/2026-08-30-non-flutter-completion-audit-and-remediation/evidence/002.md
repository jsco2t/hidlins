# Task 002 Evidence: Vault, Entry, and Generator Repairs

Completed: 2026-08-31T02:50:04Z

Approved plan SHA-256: `c6b9b58c39c4c9e9a14d79451097e7402f3faab7b9ea1fb5185004f3b423cd7d`

## Repair and review

- Fixed `HIDLINS-DEAD-001`, the only ledger finding owned by Task 002.
- Removed the unused crate-private `Keyfile::bytes` accessor, its blanket dead-code suppression, and the false claim that a future unlock module would consume it.
- Confirmed the live boundary remains `KeyfileMaterial::from_keyfile`, which clones caller-owned bytes or reads path bytes into its own `Zeroize + ZeroizeOnDrop` buffer before adapting them to `keepass-rs`.
- Primary review confirmed the change reduces the secret-bearing API surface without moving responsibility, weakening zeroization, changing KDBX behavior, or introducing a dependency.

## Test-forward evidence

This was a structural dead-surface removal, not a behavior defect, so fabricating a failing runtime test would not provide value. Before the edit, the existing `unlock::tests::build_with_keyfile_bytes_succeeds` adapter characterization and `create_vault_with_keyfile_succeeds_and_opens_with_keyfile` save/reopen integration test both passed. They pass unchanged after removal, proving the deleted accessor was not the active unlock path. The static defect changed from a matching `#[allow(dead_code)]` future-work accessor to no match.

The full core suite additionally proves password/keyfile authentication, zeroization, KDBX save/reopen, UUID/history/attachment behavior, atomic-write fault handling, and advisory-lock behavior. The full generator suite proves CSPRNG outputs, class/length/exclusion constraints, EFF wordlist fidelity, deterministic test seams, and distribution behavior.

## Task validation results

| Command | Result |
| --- | --- |
| `cargo test -p hidlins-core --offline --locked` | PASS; 148 unit tests passed, 1 environment-mutating test ignored, all default integration/doc tests passed |
| `cargo test -p hidlins-genpw --offline --locked` | PASS; unit, distribution, US-020..023, and doc tests passed |
| `make interop` | PASS; US-090/091/092 KeePassXC round trips |
| `make interop-entry` | PASS; US-010/012/013/014/016/018. Optional `oathtool` cross-check skipped because the executable is absent; KeePassXC assertions passed. |
| `git diff --check` | PASS |

## Standard gate results

| Command | Result |
| --- | --- |
| `make check` | PASS |
| `make deny` | PASS: advisories, bans, licenses, and sources all OK |
| `make audit` | PASS; 467 dependencies scanned |
| `make doc` | PASS with rustdoc warnings denied |

The separately assigned no-default security warning remains visible in `make check` and is owned by Task 003. Supply-chain informational warnings remain as documented in Task 001 evidence.

Final plan-integrity and whitespace checks passed.
