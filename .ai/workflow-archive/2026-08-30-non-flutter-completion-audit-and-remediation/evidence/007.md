# Task 007 Evidence: Build, Supply-Chain Reconciliation, and Documentation Repairs

Completed: 2026-08-31T13:06:38Z

Approved plan SHA-256: `c6b9b58c39c4c9e9a14d79451097e7402f3faab7b9ea1fb5185004f3b423cd7d`

## Repair and primary review

- Closed `HIDLINS-DOC-001`. Package metadata and live repository documentation now describe the shipped S3 transport, complete tabbed TUI, complete CLI argument surface, registry extensions, OS-event test targets, and TUI-wired sync activity callback.
- Removed the obsolete empty `hidlins-sync::activity` scaffold. The shipped `SyncOptions::on_activity` callback is exercised by sync tests and wired by the TUI to its auto-lock controller; retaining a second deferred wrapper was misleading and added no behavior.
- Tightened `deny.toml` and contributor guidance to the authoritative license list by removing `Apache-2.0 WITH LLVM-exception`. The complete targeted dependency graph passes without it; no exception or clarification was added.
- Reconciled every direct dependency in `.ai/workflow/dependency-review.md`. Tasks 002–007 introduced no dependency/version/feature change beyond Task 001. `tui-input` is retained because it serves five production single-line overlay families while adding no framework family beyond already-required ratatui/crossterm/Unicode dependencies; the local text area solves a distinct multiline problem.
- Verified that external `hex` remains absent from the all-feature/all-target graph, the TUI direct `anyhow` edge remains absent, and only the accurately documented Flutter bridge transitive still resolves `anyhow`. Patched `event-listener 5.4.2`, `lru 0.18.3`, and `chacha20 0.10.2` remain in place; their affected versions are absent.
- Reviewed every non-Flutter CI assertion against the Makefile. Repository checks use existing `make` targets; raw CI commands are runner/tool provisioning or the explicit telemetry setup immediately rechecked by `make app-check`. No CI/Make target drift or missing recurring command was found, so no new harness or dependency was introduced.
- Recorded exact external notebook corrections in `.ai/workflow/notebook-corrections.md` without modifying the notebook. Corrected the internal ledger's RST-CRED-1 evidence to name the implemented API boundary rather than a nonexistent CLI password-change flow.

## Test-forward/static evidence

These repairs changed policy, comments, metadata, and removed an empty unreferenced module; they did not change product behavior. A failing runtime test would therefore be artificial. The pre-change static audit was the red substitute and found:

- shipped crate descriptions still saying S3 “lands” later and the TUI is a placeholder;
- current CLI/TUI/core/security comments claiming implemented work was deferred;
- the empty deferred sync activity module despite the live callback implementation;
- contributor docs claiming OS-event tests run zero cases;
- `Apache-2.0 WITH LLVM-exception` present in both the executable deny policy and contributor allow-list, contrary to the authoritative rule.

After repair, targeted stale-claim, conflict-marker, direct-`anyhow`/`hex`, and license-policy searches pass. The tightened policy then passed `make deny`, proving no current targeted dependency relies on the removed allowance. Warning-denying compilation, tests, and rustdoc validate the deleted empty module and all doc links.

## Task validation results

| Command | Result |
| --- | --- |
| `git diff --check` | PASS |
| `make completions-check` | PASS: bash, zsh, and fish artifacts unchanged |
| `make snapshots-check` | PASS: all 13 TUI goldens unchanged |
| `make vendor-patches` | PASS: exact-version audited patches and checksums consistent |
| Static stale-claim/conflict/license/direct-dependency checks | PASS; only intentional roadmap placeholders and historical records remain |
| `make doc` | PASS with rustdoc warnings denied |

## Standard gate results

| Command | Result |
| --- | --- |
| `make check` | PASS: formatting, warning-denying clippy, workspace build/tests, doctests, macOS containment, and feature gates |
| `make deny` | PASS under the narrower allow-list: advisories, bans, licenses, and sources all OK |
| `make audit` | PASS: 467 dependencies scanned against 1,233 RustSec advisories; no vulnerabilities reported |
| `make doc` | PASS with rustdoc warnings denied |

Cargo-deny's informational output remains fully reconciled: `allo-isolate` has a verified vendored Apache-2.0 license file, duplicate versions are upstream-driven and inventoried, and Unicode-DFS-2016 is an allowed but currently unmatched license. No warning was suppressed.
