# Evidence — Task 009

## Implementation

- Implemented by: main
- Attempt count: 1

## Files changed

- Removed the former `02-local-display/` and `03-screen-reader/` trees under
  both `features/tui-skeleton/verifications/` and
  `features/tui-enhancements/verifications/` in the authoritative notebook.
- Removed the TUI-enhancements manual-only verification `fixtures/` and
  `scratch/` directories.
- Added central notebook documents
  `verifications/tui-real-terminal-matrix.md` and
  `verifications/tui-screen-readers.md`.
- Updated the central verification index and coverage matrix; both TUI feature
  indexes, task trackers, phase/task documents, and verification indexes; and
  the repository's `AGENTS.md`, `.ai/workflow/tui-verification-coverage.md`,
  and `.ai/workflow/notebook-corrections.md`.

## Acceptance criteria

- [x] Both features' previous `02-local-display/` and `03-screen-reader/`
      manual-test trees are absent.
  - Evidence: all four exact `test ! -d ...` task validations passed.
- [x] Manual-only fixtures and scratch artifacts are absent.
  - Evidence: explicit absence checks passed for both features' `fixtures/`
    and `scratch/` paths; the remaining verification files are indexes and
    historical headless command references.
- [x] The central terminal matrix contains only the requested Konsole,
      actual-Pop!_OS-terminal, and macOS rows and only irreducible emulator
      observations.
  - Evidence: the three-row table covers startup/restoration, 60×16
    legibility, native selection/mouse capture, and color/accessibility smoke;
    it redirects deterministic application behavior to `make
    test-tui-contracts` and `make check`.
- [x] The central screen-reader document contains exactly one Linux
      Orca/AT-SPI case and one macOS VoiceOver case.
  - Evidence: Case 1 is Linux Orca through AT-SPI and Case 2 is macOS
    VoiceOver; both require password non-echo and absence of raw
    control/escape-sequence speech noise.
- [x] Retained instructions are portable.
  - Evidence: the setup resolves `REPO_ROOT` with `git rev-parse
    --show-toplevel` before assigning isolated `HOME`; scans found no
    `/Users/jason`, `/home/jason`, or `GNOME Terminal` in retained TUI
    verification instructions.
- [x] Current indexes, mappings, and status surfaces contain no dangling old
      suite references or stale mandatory-manual-gate claims.
  - Evidence: targeted scans of the current central and feature indexes,
    READMEs, and task trackers found no references to the deleted
    `02-local-display` or `03-screen-reader` paths.
- [x] T7.4 and T5.3 are complete based on Task 008 automated evidence without
      claiming external execution.
  - Evidence: the task documents and live indexes say the compact external
    cases are optional, non-gating, and unexecuted.
- [x] Repository standard quality gates remain green.
  - Evidence: all task and standard-gate commands below passed.

## Main-thread review

Reviewed the central matrix and screen-reader cases, surviving verification
trees, central coverage/index surfaces, both feature verification indexes, and
the T7.4/T5.3 task-tracking surfaces. Confirmed the exact three-row/two-case
scope, portable setup ordering, absence of brand-specific accessibility
prerequisites, and honest non-execution language. Fixed two high-confidence
status inconsistencies discovered during review: `31/32 done` in the
tui-skeleton feature index and `29/29` in its Phase-7 completion sentence now
both read 32/32.

## Task-specific validation

| Command | Result |
| --- | --- |
| `test ! -d "$HOME/Developer/sources/personal/notebook/projects/hidlins/features/tui-skeleton/verifications/02-local-display"` | PASS |
| `test ! -d "$HOME/Developer/sources/personal/notebook/projects/hidlins/features/tui-skeleton/verifications/03-screen-reader"` | PASS |
| `test ! -d "$HOME/Developer/sources/personal/notebook/projects/hidlins/features/tui-enhancements/verifications/02-local-display"` | PASS |
| `test ! -d "$HOME/Developer/sources/personal/notebook/projects/hidlins/features/tui-enhancements/verifications/03-screen-reader"` | PASS |
| `rg -n 'Konsole\|Pop!_OS\|macOS\|Orca\|AT-SPI\|VoiceOver' "$HOME/Developer/sources/personal/notebook/projects/hidlins/verifications/tui-real-terminal-matrix.md" "$HOME/Developer/sources/personal/notebook/projects/hidlins/verifications/tui-screen-readers.md"` | PASS |
| `make test-tui-contracts` | PASS — 3 scripted journeys and 4 accessibility contracts |
| `make check` | PASS |

## Standard quality gate

| Command | Result |
| --- | --- |
| `make check` | PASS |
| `make deny` | PASS — advisories, bans, licenses, and sources OK; configured non-fatal duplicate/unmatched-license warnings remain |
| `make audit` | PASS — 467 locked dependencies scanned with no vulnerability finding |
| `make doc` | PASS — rustdoc warnings denied |

## Remaining concerns

The compact Konsole, Pop!_OS-terminal, macOS-terminal, Linux Orca/AT-SPI, and
macOS VoiceOver cases are deliberately unexecuted, optional real-environment
confidence checks. No pass is claimed for them.
