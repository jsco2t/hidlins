# Task 005 Evidence: CLI Surface Repairs

Completed: 2026-08-31T03:43:34Z

Approved plan SHA-256: `c6b9b58c39c4c9e9a14d79451097e7402f3faab7b9ea1fb5185004f3b423cd7d`

## Repair and primary review

- The Task-001 audit found no independent CLI behavior defect. Task 005 therefore completed the CLI prerequisite of `HIDLINS-ARCH-002` without changing public command behavior.
- Added owned, secret-free `EntrySummary` and `GroupSummary` core query records. They expose navigation metadata only; usernames, passwords, URLs, notes, attachment bytes, and custom-field values are absent.
- Added narrow core queries for read-only entry access, entry summaries, case-insensitive title resolution, and group summaries.
- Migrated CLI add/get/list/search/scope resolution away from `Vault::database`, `VaultReadOnly::database`, raw `keepass-rs` entries/groups, raw field constants, and database iterators. A source boundary check finds no such traversal under `crates/hidlins-cli/src`.
- Preserved traversal order, group labels, tag filtering, expiration behavior, title ambiguity messages, search scores/indices, JSON schemas, password redaction, and exit codes.
- Indexed search-result summaries once, removing the inherited O(results × entries) display-enrichment walk.
- During the pre-refactor characterization run, the vendored Keepass patch runner exposed a non-idempotent insertion and duplicated `rebuild_attachment_references`. Replaced that patch operation with an audited `insert_once` primitive, removed the duplicate, refreshed the checksum, and ran `make vendor-patches` twice successfully. No dependency, version, feature, license allowance, advisory exception, or unsafe code was added.

Primary review traced all CLI entry query paths, secret egress controls, prompt sources, JSON serialization, exit mapping, sync-conflict mapping, and completion generation. The TUI and final raw-core seam remain explicitly owned by Task 006; `HIDLINS-ARCH-002` is therefore recorded as partially fixed rather than closed.

## Test-forward evidence

Before the structural refactor, `entry_query_commands_preserve_group_and_title_contracts` was added and passed against the raw traversal implementation. It characterizes:

- list output retaining the containing group name;
- case-insensitive group scope resolution;
- scoped search returning only the group-subtree match with the same group label;
- case-insensitive title ambiguity returning exit 1 and listing both UUIDs.

The same exact test passed after migration to the owned core query records. Existing command-boundary suites continued to cover secure redaction, explicit password display, entry lifecycle, JSON schemas, documented exit codes, sync wiring/conflict mapping, and honest SSH slots.

## Task validation results

| Command | Result |
| --- | --- |
| `cargo test -p hidlins-cli --offline --locked` | PASS: 128 unit tests and all default command-boundary suites; 12 entry integrations include the new characterization |
| `cargo test -p hidlins-cli --test cli_master_password --offline --locked -- --ignored --test-threads=1` | PASS: 4 serialized secure-prompt/env rejection tests |
| `make completions-check` | PASS: bash, zsh, and fish committed completions match generation |
| `make vendor-patches` (twice) | PASS: exact-version vendor patching is idempotent and checksums remain consistent |
| CLI raw-boundary `rg` check | PASS: no database access, raw iterators, or raw Keepass model usage in production CLI source |

## Standard gate results

| Command | Result |
| --- | --- |
| `make check` | PASS: formatting, warning-denying clippy, workspace build/tests, macOS containment, clipboard/API/logind feature gates, and no-default security build |
| `make deny` | PASS: advisories, bans, licenses, and sources all OK |
| `make audit` | PASS: 467 dependencies scanned against 1,226 RustSec advisories |
| `make doc` | PASS with rustdoc warnings denied |

The pre-existing cargo-deny informational warnings remain those documented in Task 001: the verified `allo-isolate` license file, upstream-driven duplicates, and unmatched Unicode-DFS allowance. Task 005 introduced no supply-chain change.
