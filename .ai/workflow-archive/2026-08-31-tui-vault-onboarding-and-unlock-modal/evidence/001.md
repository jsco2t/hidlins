# Task 001 Evidence: Transactional Vault Registration

Completed: `2026-09-01T03:36:18Z`

## Test-forward cycle

- Red: `cargo test -p hidlins-core --offline --locked registry::tests`
  failed with `E0599` because `VaultRegistry::register_and_save` did not yet
  exist.
- Green: the same focused command passed after the implementation (`18 passed`).

## Implementation

- Added `VaultRegistry::register_and_save` using the existing exclusive lock,
  reload, duplicate check, and atomic serializer.
- Caller state is replaced only after the atomic save succeeds.
- Added coverage for missing-registry round-trip fidelity, stale-caller merge,
  and duplicate-name disk/caller preservation.
- No dependency, feature, unsafe-code, schema, or license-policy change.

## Review

Main-thread diff review confirmed that the latest registry is loaded only after
the lock is acquired, `save_unlocked` avoids recursive locking, and no state is
assigned before successful persistence. No findings remained.

## Validation

- `cargo test -p hidlins-core --offline --locked registry::tests` — PASS
- Frozen dependency fingerprint — PASS
- `make check` — PASS
- `make deny` — PASS (existing warnings only; advisories, bans, licenses, and sources all OK)
- `make audit` — PASS
- `make doc` — PASS

