# Task 003 Evidence: Branded Accessible Startup Modal

Completed: `2026-09-01T04:06:42Z`

## Test-forward cycle

- Red: the first compact-accessible contract failed because the old prompt had
  no semantic `HIDLINS` heading, explicit focused-field label, or colon-labeled
  actions.
- Green: the shared renderer satisfies that contract and the complete startup
  journey/accessibility suite.
- Red: three new startup snapshot tests failed closed while their goldens were
  absent; the reviewed goldens were then generated through the existing
  `make test-update-snapshots` target.

## Implementation

- Replaced three independent startup screen renderers with one responsive
  modal for onboarding, configured-vault selection, and password entry.
- Stored and rendered the requested 13-row ASCII art exactly at normal 80×24
  and 60×16 geometry; compact and accessible layouts deliberately replace it
  with a semantic `HIDLINS` heading.
- Added textual focus, selection, masked-password, status/error, retry, Open,
  Unlock, Back, Continue, and Exit carriers in row-major review order.
- Added stateful list scrolling so long configured-vault lists keep the selected
  row visible.
- Sanitized control characters from user-controlled reviewable display text.
- Updated current crate and README startup documentation.
- Added no dependency, feature, unsafe code, asset, or license allowance.

## Automated journeys and contracts

- Added real fast-KDF KDBX journeys for authenticate-before-register,
  wrong-password recovery, successful save/reload, concurrent registration
  recovery, direct unlock, multi-vault navigation, Escape-to-same-row,
  alternate-vault unlock, and startup exit.
- Added buffer contracts for exact art rows, normal/accessible/compact semantic
  parity, long-list visibility, password and path non-leakage, non-color text
  carriers, and absence of raw escape/C0/C1 controls across startup states.
- Added normal onboarding, normal multi-vault, and accessible compact password
  goldens using the existing hand-rolled snapshot harness.

## Review and repairs

- The requested architecture review found no Critical or Important finding at
  confidence 80/100 or higher. State, orchestration, persistence, and rendering
  ownership remain separated along the established Rust-core/thin-UI boundary.
- Attempt 1's strict Clippy review found a potentially truncating literal-width
  cast and an idiom warning. Attempt 2 replaced both without behavioral change.

## Validation

- `make test-tui-contracts` — PASS (6 journeys, 9 accessibility contracts)
- `make snapshots-check` — PASS (16 goldens current)
- `cargo test -p hidlins-tui --offline --locked` — PASS (438 passed, 4 ignored)
- Frozen dependency fingerprint — PASS
- `make check` — PASS
- `make deny` — PASS (existing warnings only; advisories, bans, licenses, and sources all OK)
- `make audit` — PASS
- `make doc` — PASS
