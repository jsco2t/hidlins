# Task 002 Evidence: Startup Onboarding State Machine

Completed: `2026-09-01T03:54:56Z`

## Test-forward cycle

- Red: focused application tests initially failed to compile because the empty,
  single-vault, and multiple-vault startup phases and pending-registration
  transitions did not yet exist.
- Green: `cargo test -p hidlins-tui --offline --locked --lib app::tests`
  passed with all 190 focused application tests after implementation.
- Existing session ownership characterization remained green (`2 passed`).

## Implementation

- Added explicit onboarding, configured-vault picker, and typed unlock-origin
  states with cardinality-based startup routing.
- Added portable path resolution and validation for trimmed absolute, relative,
  and leading-`~/` paths without shell expansion.
- Authenticate-first onboarding now registers only after a successful vault
  open, using Task 001's transactional registry operation; persistence failures
  drop the opened vault and reload authoritative registry state.
- Multiple-vault prompts return to the same selected row on Escape, resolve the
  vault by name at unlock time, and isolate authentication attempts by prompt.
- Preserved three authentication attempts, direct `--vault` behavior,
  lock/re-entry, field typing, and global `Ctrl+Q` exit behavior.
- Removed the obsolete empty-registry terminal error.
- Added no dependency, feature, unsafe code, or license allowance.

## Review and repairs

- Architecture review identified parallel optional prompt fields that admitted
  invalid combinations. They were replaced with the `UnlockOrigin` enum so
  direct, picker, and pending-registration prompts carry only valid state.
- Attempt 1 found formatter drift after that structural repair.
- Attempt 2 found mechanical lint issues and an unused hint helper; the final
  repair retained the shared hint renderer while making startup actions
  explicit in text.
- Main-thread review found no remaining in-scope issue.

## Validation

- `cargo test -p hidlins-tui --offline --locked --lib app::tests` — PASS
- `cargo test -p hidlins-tui --offline --locked --lib session::tests` — PASS
- Frozen dependency fingerprint — PASS
- `make check` — PASS
- `make deny` — PASS (existing warnings only; advisories, bans, licenses, and sources all OK)
- `make audit` — PASS
- `make doc` — PASS
