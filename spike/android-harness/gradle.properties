# Mirrors app/android/gradle.properties flag-for-flag (minus Flutter bits)
# so the spike validates the same AGP 9 configuration surface T8.1 will use.
org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G
android.useAndroidX=true
android.newDsl=false
android.builtInKotlin=false
