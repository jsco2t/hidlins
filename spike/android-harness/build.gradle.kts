// Versions mirror app/android/settings.gradle.kts (AGP 9.0.1, Kotlin
// 2.3.20) so the spike's findings transfer to the production build (T8.1).
plugins {
    id("com.android.application") version "9.0.1" apply false
    id("org.jetbrains.kotlin.android") version "2.3.20" apply false
}
