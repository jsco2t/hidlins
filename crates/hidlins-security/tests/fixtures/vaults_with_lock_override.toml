# US-051 per-vault override fixture: `[vault.lock] idle_timeout_seconds = 30`.
# Consumed by `tests/us_051_idle_auto_lock.rs::per_vault_override_from_vaults_toml_is_honored`
# (Phase 4 T4.2 un-ignores that case). The schema is the same one
# `hidlins-cli vault set-lock --timeout 30` writes today via
# `RegisteredVault::extra`; `vault_lock::VaultLockConfig::from_extra`
# is read back as a typed per-vault override.

version = 1

[[vault]]
name = "personal"
path = "/tmp/personal-override.kdbx"
created_at = "2026-05-21T10:00:00-06:00"

[vault.lock]
idle_timeout_seconds = 30
